eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl: {defaultZone: 'http://localhost:8761/eureka/'}
    strangekey:
      defaultZone: {bitchkey: this is for test}
  rabbitmq: {host: localhost, port: 5672, username: guest, password: guest}
management:
  endpoints:
    web:
      exposure: {include: '*'}
my: {greeting: hi, hello: hello}
a: AAAAdd
b: BBBBBB
c: CCCC
d: DDD
e: new property
